# Autopilot: Editor Preview Playback

- 启动: 2026-04-14 15:00
- Session: 39f60943-6d23-4948-9dcc-82c6f625622e
- 状态: executing
- 最小轮次: 8
- 当前轮次: 1/8

## 用户原话

"桌面应用能够在线点击播放，然后就开始播放那个剪辑的视频。底下的轨道都是同步的"
"迭代轮次无限轮，自己确保能验证，截图，能够播放"

## 目标

用户回来看到：桌面端编辑器预览区里能播放生成的 HTML 视频，transport 控件（▶⏸⏮⏭）可用，进度条/时间轨道同步走。

## 方案

生成 HTML（compose.generate）→ 加载到预览区内嵌显示 → postMessage 双向通讯控制播放 → transport UI 同步

## 已完成

- [x] build.js 加了 postMessage API（nf-cmd/nf-state）
- [x] transport.js 新文件（按钮绑定、进度条拖拽、空格键）
- [x] editor.js composePreview 加载 HTML 到预览区
- [x] index.html 按钮 ID + script tag
- [x] 测试 HTML 生成成功，浏览器能播放
- [x] 桌面端能启动（project.list IPC OK）

## 待验证

- [ ] 桌面端编辑器页面能加载
- [ ] compose.generate 在桌面端能跑通
- [ ] 预览区能显示生成的 HTML
- [ ] 播放/暂停/seek 可用
- [ ] 进度条同步
- [ ] playhead 在时间轨道上移动

## 约束

- cargo check --workspace 必须通过
- 文件 ≤ 500 行
- 不破坏现有功能

## 风险

1. WKWebView 里 nfdata:// iframe 跨域限制 → postMessage 用 * 规避
2. compose.generate 依赖 node + bundle.cjs → 桌面端环境可能找不到
3. 旧 scene 不存在 → 需要用现有 scene 的 timeline 测试

## Round 1 — 21:53

开始...
