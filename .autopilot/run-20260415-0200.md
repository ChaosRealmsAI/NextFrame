# Autopilot: v0.6 DOM 引擎预览

- 启动: 2026-04-15 02:00
- Session: 39f60943-6d23-4948-9dcc-82c6f625622e
- 状态: executing
- 最小轮次: 10
- 当前轮次: 7/10

## 已完成

- [x] Walking skeleton — scene-bundle + preview-engine + editor 集成
- [x] DOM 引擎播放 + 暂停 + seek + 时间码同步
- [x] 点击选中联动 inspector
- [x] 空格键 play/pause
- [x] setInterval fallback for WKWebView
- [x] 6 个场景 index.js 恢复 + bundle 生成

## 8/8 BDD 全通过

EP-1~EP-8 全部验证通过。

## 待完成：前端布局重写

### 问题

当前 HTML 结构跟 v0.6 原型不一致：
1. 左侧显示的是**轨道 layers**，原型要的是**多个视频（剪辑）列表**
2. 右侧 inspector 还在，原型没有
3. 整体没有组件化复用感，体验差
4. Tab 名称不对（"剪辑" → 应该是 "智能剪辑"）

### 下一步（R8）

**用户要求主 agent 亲自改前端，不委派。**

需要做的：
1. 重写 `index.html` 编辑器 tab 的 HTML 结构，匹配原型：
   - `.panel-left` = 视频列表（多个剪辑，不是 layers）
   - `.panel-center` = preview + transport + timeline
   - 去掉 `.panel-right` inspector
2. 重写 `editor.css` 匹配原型样式
3. 改 `editor.js` 绑定新的 DOM 结构
4. Tab 改名为 "智能剪辑"

### 原型参考

`spec/prototypes/v0.6/editor-preview.html` — 1006 行，完整的两栏布局。

关键 class 名：
- `.panel-left.glass` → 视频列表 240px
- `.panel-center` → preview + transport + timeline
- `.video-list` → 视频卡片列表
- `.preview-container` → 16:9 预览区
- `.transport` → SVG 按钮 + 时间码
- `.timeline` → 标尺 + 可滚动轨道 + playhead
