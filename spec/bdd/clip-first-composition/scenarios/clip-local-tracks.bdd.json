{
  "id": "clip-local-tracks",
  "title": "One clip expands into local tracks",
  "given": "The intro clip contains component, tts, subtitle_timeline, and subtitle tracks.",
  "when": "The user selects intro.",
  "then": "The timeline shows only intro's local tracks and local anchors.",
  "human_actions": ["click intro clip", "read timeline rows", "scrub inside intro"],
  "ai_tools": [
    {
      "tool": "state",
      "command": "NEXTFRAME_HOME=examples target/debug/nf verify --project v2-showcase --composition showreel-clip-first | jq '.clips[] | select(.id==\"intro\") | .tracks'",
      "expected": "contains component, tts, subtitle_timeline, subtitle"
    }
  ]
}
