{
  "kind": "audio",
  "name": "Audio Track",
  "description": "音频轨道 · MP3/WAV · 起止时间裁剪 · 旁白 / 音效 / 背景音乐",
  "use_cases": ["旁白配音", "背景音乐", "音效"],
  "t0_visibility": 1.0,
  "z_order_hint": 0,
  "visual_channels": ["audio"],

  "params": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["src"],
    "additionalProperties": false,
    "properties": {
      "src": {"type": "string", "pattern": "^(file://|data:|https?://)"},
      "from_ms": {"type": "number", "minimum": 0},
      "to_ms": {"type": "number", "minimum": 0},
      "volume": {"type": "number", "minimum": 0, "maximum": 1}
    }
  },

  "sample": {
    "src": "file:///tmp/narration.mp3",
    "from_ms": 0,
    "volume": 1
  }
}
