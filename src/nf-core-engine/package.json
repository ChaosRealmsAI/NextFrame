{
  "name": "nf-core-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "nf-core-engine": "dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/*.ts --outdir=dist --platform=node --format=esm --target=es2022",
    "test": "npm run build && node --test dist/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
