// Expression evaluator stub — returns 0 for any expression.
// Real impl: hand-rolled PEG over +/-/*/div/ref(name) with dependency tracking.

export function evalExpr(_src: string): number {
  return 0;
}
