// TODO: expand composite layers into renderable leaf layers
const meta = { name: "expandLayer", kind: "engine", description: "Layer expansion stub" };
export function expandLayer(layer = {}) {
  // TODO: expand behavior-driven virtual layers
  return [layer];
}
export { meta };
