//! nf — NextFrame CLI entry
//!
//! Scaffold only (v0.1.1). Real subcommands land in v0.2+.
//! See `spec/versions/v0.1.1/kickoff/version-level.json`.

fn main() {
    println!("nf {} · scaffold only (v0.1.1)", env!("CARGO_PKG_VERSION"));
    println!("subcommands land in v0.2+");
}
