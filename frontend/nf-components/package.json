{
  "name": "@nextframe/nf-components",
  "version": "0.1.1",
  "private": true,
  "description": "NextFrame UI components (Web Components, zero framework).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js",
    "check": "tsc --noEmit",
    "test": "node test-w4.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "pixelmatch": "^5.3.0",
    "playwright": "^1.52.0",
    "pngjs": "^7.0.0",
    "typescript": "^5.5.0"
  }
}
