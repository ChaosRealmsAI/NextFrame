#!/bin/bash
# v2.0 rebuild in progress — legacy lint checks disabled.
# Old src archived to legacy-v08/ (not compiled). New src/ is empty skeleton.
# This script will be rebuilt as new crates are filled in.
set -e
echo "[lint-all] v2.0 rebuild in progress — checks deferred until new src/ has content"
exit 0
